// SPDX-License-Identifier: MIT

pragma solidity ^0.8.4;

contract TypeCast {
// you can pass any data on abi.encode
// you can't encode struct
// constructing domain operators use abi.encodePacked
string(abi.encodePacked("temi", "tayo"))
string(abi.encodePacked("temi", "tayo), ",")
// append => appendInt
mapping(uint => mapping(uint => Names) noToName); 
// import file means you have access to contract

// Diamond standard optimization...remove unused code

}