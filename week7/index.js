function average(numbers) { // provide an array of numbers: expect an array
    for(let i=0; i < arrayOfNumbers.length; i++) {
        return arrayOfNumbers[i];
    }
}

function meanOfMultipleArrays(allArrays) {
    let newArrr = []
    for(let i = 0; i < arrayOfNumbers.length; i++) {
        let avr = average(allArrays[i]);
        newArr.push(avr)
    }
    console.log(newArrr);
    console.log(average(newArrr));
}

// equivalent of abi.encode in Javascript 